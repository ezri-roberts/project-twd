#ifndef ENEMY_TYPES_H
#define ENEMY_TYPES_H

typedef enum {
	ZOMBIE,
	SKELETON,
	SPIDER,
	SLIME
} EnemyType;

typedef enum {
	TYPE_NONE,
	TYPE_UNDEAD,
	TYPE_FOREST,
	TYPE_PLAINS
} ElementType;

#endif
